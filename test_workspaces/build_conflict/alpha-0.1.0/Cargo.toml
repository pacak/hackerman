[package]
name = "alpha"
version = "0.1.0"
edition = "2024"

[dependencies]
codec = { path = "../codec-0.1.0", features = ["decode"] }

[build-dependencies]
codec = { path = "../codec-0.1.0", features = ["decode", "encode"] }
pass1 = { path = "../pass1-0.1.0" }

