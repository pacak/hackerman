[package]
name = "gamma"
version = "0.1.0"
edition = "2024"

[dependencies]
codec = { path = "../codec-0.1.0", features = ["decode"] }

[dev-dependencies]
codec = { path = "../codec-0.1.0", features = ["test"] }

