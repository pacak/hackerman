[package]
name = "rand"
version = "0.1.0"
edition = "2024"

[dependencies]
rand_chacha = { path = "../rand_chacha-0.1.0", optional = true, default-features = false }

[features]
default = ["std"]
std = ["rand_chacha?/std"]
std_rng = ["dep:rand_chacha"]
