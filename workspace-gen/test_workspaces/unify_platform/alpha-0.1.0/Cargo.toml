[package]
name = "alpha"
version = "0.1.0"
edition = "2024"

[dependencies]
widget = { path = "../widget-0.1.0", features = ["base"] }

[target.'cfg(target_os = "android")'.dependencies]
widget = { path = "../widget-0.1.0", features = ["android"] }

