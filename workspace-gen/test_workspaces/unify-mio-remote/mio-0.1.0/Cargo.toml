[package]
name = "mio"
version = "0.1.0"
edition = "2024"

[dependencies]
log = { path = "../log-0.1.0", optional = true }

[features]
default = ["log"]
log = ["dep:log"]
net = []
os_ext = ["os_poll"]
os_poll = []
